- name: package installation
  hosts: local
  connection: local
  become: true
  tasks:
  - name:
    ansible.builtin.package:
      name: "{{ item }}"
      state: present
    loop:
    - nginx
    - mysql
    - zip


